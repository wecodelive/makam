PORT=3000
NODE_ENV=production
JWT_SECRET=your_jwt_secret
JWT_EXPIRE=7d
JWT_COOKIE_EXPIRE=7

# Frontend domains allowed to call this API (comma-separated)
CORS_ORIGIN=https://makamlive.netlify.app

# Use 'none' when frontend/backend are on different domains
COOKIE_SAMESITE=none
