# Frontend API base URL for deployed environments.
# Leave empty when frontend and backend share the same domain and /api path.
VITE_API_BASE_URL=

# Local dev proxy target used by Vite dev server only.
VITE_API_PROXY_TARGET=http://localhost:3000
